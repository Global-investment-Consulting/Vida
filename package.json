{
  "name": "vida-mvp-nodb",
  "version": "0.5.0",
  "description": "Vida MVP (JSON -> UBL) minimal API (no DB)",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=18.17"
  },
  "scripts": {
    "start": "node server.js",
    "start:ci": "node server.js",
    "wait:api": "node scripts/wait-for-api.mjs",
    "smoke:curl": "curl -fsS http://127.0.0.1:3001/healthz | grep -q \"ok\"",
    "smoke": "npm run smoke:curl"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
