{
  "name": "vida-mvp-nodb",
  "version": "0.5.0",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "start:ci": "node server.js",
    "wait:api": "node scripts/wait-for-api.mjs",
    "smoke:curl:linux": "curl -fsS http://127.0.0.1:3001/healthz | grep -q ready",
    "smoke:curl:win": "powershell -NoProfile -Command \"$r = & curl.exe -fsS http://127.0.0.1:3001/healthz; if ($LASTEXITCODE -ne 0 -or ($r | Select-String -SimpleMatch 'ready') -eq $null) { exit 1 }\""
  }
}
