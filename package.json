{
  "name": "vida-mvp-nodb",
  "version": "0.5.0",
  "private": true,
  "description": "Vida MVP (no DB) — API with stable /healthz and cross-OS CI support",
  "main": "server.js",
  "license": "MIT",
  "scripts": {
    "start": "node server.js",
    "start:ci": "node server.js",
    "wait:api": "node scripts/wait-for-api.mjs",
    "test": "echo \"No tests configured\" && exit 0"
  },
  "engines": {
    "node": ">=18.17"
  }
}
