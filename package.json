{
  "name": "vida-mvp-nodb",
  "version": "0.5.0",
  "private": true,
  "type": "module",
  "description": "ViDA MVP (no DB) — JSON → UBL 2.1 → PEPPOL scaffolding + smoke tests",
  "main": "server.js",
  "engines": {
    "node": ">=18.17 <23",
    "npm": ">=9"
  },
  "scripts": {
    "start": "node server.js",
    "start:ci": "node server.js",
    "wait:api": "node scripts/wait-for-api.mjs",
    "smoke:linux": "curl -fsS http://127.0.0.1:3001/healthz | grep -q \"ok\"",
    "smoke:win": "powershell -NoProfile -Command \"(Invoke-WebRequest -UseBasicParsing http://127.0.0.1:3001/healthz).Content -match 'ok' | Out-Null; if (-not $?) { exit 1 }\"",
    "lint": "echo \"(no lint configured)\"",
    "test": "echo \"(no tests configured)\""
  },
  "dependencies": {
    "body-parser": "^1.20.3",
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "node-fetch": "^3.3.2",
    "xmlbuilder2": "^3.1.1"
  }
}
