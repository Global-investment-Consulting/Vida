# .env.example
PORT=3001
API_KEY=key_test_12345
VAT_RATE=0.21

# Storage selector: "false" = file JSON store (default), "true" = Prisma DB
USE_DB=false

# Prisma (SQLite dev). You can change to Postgres later.
DATABASE_URL="file:./dev.db"
